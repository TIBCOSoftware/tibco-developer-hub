# =============================================================================
# TP-Generic-Supplements-Pipeline.yml
# -----------------------------------------------------------------------------
# Purpose  : Build a TIBCO Platform supplement (EMS or SAP), validate artefact
#            files from the TCI repo, then deploy the resulting image to the
#            chosen Kubernetes cluster (AKS / EKS / GKE).
#
# Stages   : 1. BuildSupplement    – validates inputs, builds JSON, checks files
#            2. DeployToCluster    – cloud-specific kubeconfig + kubectl
#
# Cloud providers supported:
#   aks  → Azure Kubernetes Service   (uses az CLI)
#   eks  → Amazon Elastic Kubernetes  (uses aws CLI)
#   gke  → Google Kubernetes Engine   (uses gcloud CLI)
#
# Variable group required: Tibco_Build_Deploy_Secrets
#   - AZURE_PAT   : Azure DevOps Personal Access Token (for git clone)
#   - API_TOKEN   : TIBCO Platform Bearer token   (used in the TODO cURL block)
# =============================================================================

name: TIBCO_BUILD_QA

trigger:
  - TIBCO_BUILD_QA   # Runs automatically when this branch receives a push

pool:
  name: RPC-TIBCO-AKS-Pool   # Default pool; overridable per job if needed

# =============================================================================
# VARIABLE GROUPS
# =============================================================================
variables:
  - group: Tibco_Build_Deploy_Secrets   # Provides: AZURE_PAT, API_TOKEN

# =============================================================================
# PARAMETERS
# =============================================================================
parameters:

  # ---------------------------------------------------------------------------
  # TIBCO PLATFORM BUILD INPUTS
  # ---------------------------------------------------------------------------
  - name: depName
    displayName: 'Dependency Name'             # TIBCO dependency (artefact) name
    type: string

  - name: depVersion
    displayName: 'Dependency Version'          # Semantic version of the dependency
    type: string

  - name: supplementName
    displayName: 'Supplement Name'             # Which TIBCO supplement to include
    type: string
    default: ems
    values:
      - ems   # TIBCO EMS (Enterprise Message Service) supplement
      - sap   # SAP adapter supplement

  - name: buildName
    displayName: 'Build Name'                  # Human-readable label for this build
    type: string

  - name: tags
    displayName: 'Tags (comma-separated)'      # Metadata tags stored on the build record
    type: string

  # ---------------------------------------------------------------------------
  # SOURCE ARTEFACT REFERENCES
  # ---------------------------------------------------------------------------
  - name: substvar
    displayName: 'SUBSTVAR file name'          # Substitution-variable profile file name
    type: string

  - name: ear
    displayName: 'EAR file name'               # TIBCO EAR deployment archive file name
    type: string

  - name: eardirectorypath
    displayName: 'Repo directory path for EAR file'       # Path relative to repo root
    type: string

  - name: substvardirectorypath
    displayName: 'Repo directory path for SUBSTVAR file'  # Path relative to repo root
    type: string

  # ---------------------------------------------------------------------------
  # CLOUD PROVIDER SELECTOR
  # ---------------------------------------------------------------------------
  - name: cloudProvider
    displayName: 'Target Cloud Provider'
    type: string
    default: 'aks'
    values:
      - aks    # Azure Kubernetes Service
      - eks    # Amazon Elastic Kubernetes Service
      - gke    # Google Kubernetes Engine

  # ---------------------------------------------------------------------------
  # KUBERNETES DEPLOYMENT
  # ---------------------------------------------------------------------------
  - name: imageName
    displayName: 'Container Image to Deploy'              # Full image ref, e.g. myacr.azurecr.io/app:1.0.0
    type: string

  - name: k8sNamespace
    displayName: 'Kubernetes Namespace'                   # Target namespace for the workload
    type: string
    default: 'default'

  - name: k8sDeploymentName
    displayName: 'Kubernetes Deployment Name'             # Name of the K8s Deployment resource
    type: string
    default: 'tp-app'

  - name: k8sContainerName
    displayName: 'Kubernetes Container Name in Deployment'  # Container spec name inside the pod
    type: string
    default: 'tp-container'

# =============================================================================
# STAGE 1 – BUILD TIBCO SUPPLEMENT (EMS / SAP)
# =============================================================================
stages:
  - stage: BuildSupplement
    displayName: 'Stage 1 – Build TIBCO Supplement (EMS / SAP)'
    jobs:
      - job: BuildSupplementJob
        displayName: 'Prepare Supplement JSON Payload & Validate Files'
        pool:
          name: RPC-TIBCO-AKS-Pool
        steps:
          - checkout: none   # Repo is cloned manually below to a known path

          # -------------------------------------------------------------------
          # STEP 1 – Clone TCI repository
          # -------------------------------------------------------------------
          - script: |
              echo "============================================================"
              echo " STEP 1 – Clone TCI repository"
              echo " Branch     : TP_QA"
              echo " Supplement : ${{ parameters.supplementName }}"
              echo " Cloud      : ${{ parameters.cloudProvider }}"
              echo "============================================================"

              # Use a dedicated directory to avoid workspace conflicts
              sudo mkdir -p /home/TIBCO
              sudo chmod -R 777 /home/TIBCO
              cd /home/TIBCO

              # Remove any stale clone from a previous run
              if [ -d "TCI" ]; then
                echo "📂 Removing existing 'TCI' directory..."
                rm -rf TCI
              fi

              git clone \
                --branch TP_QA \
                "https://$(AZURE_PAT)@dev.azure.com/RPCSharedServices/TCI/_git/TCI"
            displayName: 'Clone TCI Git Repository'

          # -------------------------------------------------------------------
          # STEP 2 – Validate mandatory inputs
          # -------------------------------------------------------------------
          - script: |
              echo "============================================================"
              echo " STEP 2 – Validate mandatory inputs"
              echo "============================================================"

              ERRORS=0

              if [ -z "${{ parameters.depName }}" ]; then
                echo "❌ ERROR: 'Dependency Name' (depName) is required but not provided."
                ERRORS=$((ERRORS + 1))
              fi

              if [ -z "${{ parameters.depVersion }}" ]; then
                echo "❌ ERROR: 'Dependency Version' (depVersion) is required but not provided."
                ERRORS=$((ERRORS + 1))
              fi

              if [ -z "${{ parameters.supplementName }}" ]; then
                echo "❌ ERROR: 'Supplement Name' (supplementName) is required but not provided."
                ERRORS=$((ERRORS + 1))
              fi

              if [ "$ERRORS" -gt 0 ]; then
                echo "❌ Validation failed with $ERRORS error(s). Stopping pipeline."
                exit 1
              fi

              echo "✅ All mandatory inputs are valid."
            displayName: 'Validate Mandatory Inputs'

          # -------------------------------------------------------------------
          # STEP 3 – Build JSON payload and validate artefact files
          # -------------------------------------------------------------------
          - script: |
              echo "============================================================"
              echo " STEP 3 – Build supplement JSON payload & validate files"
              echo "============================================================"

              # ---- Convert comma-separated tags to a JSON array ----
              echo "🔧 Converting tags to JSON array..."
              IFS=',' read -ra TAG_ARR <<< "${{ parameters.tags }}"
              TAG_JSON=$(printf '"%s",' "${TAG_ARR[@]}")
              TAG_JSON="[${TAG_JSON%,}]"
              echo "   Tags JSON : $TAG_JSON"

              # ---- Assemble the supplement build request JSON body ----
              # Note: tags is wrapped as [$tag] (single string) to match the
              # existing TP supplement API contract – adjust if your API expects
              # a proper JSON array like in the Build pipeline.
              echo "🧱 Building JSON payload..."
              JSON_PAYLOAD=$(jq -n \
                --arg depName        "${{ parameters.depName }}" \
                --arg depVersion     "${{ parameters.depVersion }}" \
                --arg supplementName "${{ parameters.supplementName }}" \
                --arg buildName      "${{ parameters.buildName }}" \
                --arg tag            "$TAG_JSON" \
              '{
                dependencies: [{
                  name: $depName,
                  version: $depVersion,
                  supplement: { supplementName: $supplementName }
                }],
                tags: [$tag],
                buildName: $buildName
              }')
              echo "   Generated payload:"
              echo "$JSON_PAYLOAD" | jq .

              # ---- Resolve artefact file paths from cloned repo ----
              echo "📂 Resolving EAR and SUBSTVAR file paths..."
              EAR_FILE="/home/TIBCO/TCI/${{ parameters.eardirectorypath }}/${{ parameters.ear }}"
              SUBSTVAR_FILE="/home/TIBCO/TCI/${{ parameters.substvardirectorypath }}/${{ parameters.substvar }}"

              echo "   EAR file      : $EAR_FILE"
              echo "   SUBSTVAR file : $SUBSTVAR_FILE"

              # ---- Guard: both artefact files must exist ----
              if [ ! -f "$EAR_FILE" ]; then
                echo "❌ ERROR: EAR file not found: $EAR_FILE"
                exit 1
              fi

              if [ ! -f "$SUBSTVAR_FILE" ]; then
                echo "❌ ERROR: SUBSTVAR file not found: $SUBSTVAR_FILE"
                exit 1
              fi

              echo "✅ Both EAR and SUBSTVAR files exist."
              echo "📋 Current working directory listing:"
              pwd && ls -lrth

              # ----------------------------------------------------------------
              # TODO – TIBCO Platform Supplement Build API call
              # ----------------------------------------------------------------
              # Uncomment and configure the block below when the TP supplement
              # build endpoint is available in your environment.
              #
              # FULL_URL="https://<tp-host>/tibco-qa/public/v1/dp/builds?baseversion=<ver>&baseimagetag=<tag>"
              #
              # curl -k --fail --location "$FULL_URL" \
              #   --header "accept: application/json" \
              #   --header "Authorization: Bearer $(API_TOKEN)" \
              #   --form "request=$JSON_PAYLOAD" \
              #   --form "earFile=@${EAR_FILE}" \
              #   --form "customProfile=@${SUBSTVAR_FILE}"
              # ----------------------------------------------------------------
            displayName: 'Build Supplement JSON & Validate Artefact Files'

# =============================================================================
# STAGE 2 – CLOUD-SPECIFIC KUBERNETES DEPLOYMENT (AKS / EKS / GKE)
# =============================================================================
# This stage runs after BuildSupplement succeeds.
# Only ONE of the three cloud-provider jobs will execute, determined by
# the 'cloudProvider' parameter evaluated at queue time.
# =============================================================================
  - stage: DeployToCluster
    displayName: 'Stage 2 – Cloud-specific Kubernetes Deployment'
    dependsOn: BuildSupplement
    condition: succeeded()
    jobs:

      # ========================================================================
      # AKS – Azure Kubernetes Service
      # ========================================================================
      # Prerequisites on the agent:
      #   • Azure CLI (az) installed and logged-in, OR service-principal env vars set
      #   • kubectl installed
      # To configure kubeconfig for AKS, uncomment and fill in the az commands below.
      # ========================================================================
      - job: Deploy_AKS
        displayName: 'Deploy Supplement Image to AKS (Azure)'
        pool:
          name: RPC-TIBCO-AKS-Pool
        condition: eq('${{ parameters.cloudProvider }}', 'aks')
        steps:
          - script: |
              echo "============================================================"
              echo " AKS Deployment"
              echo " Namespace  : ${{ parameters.k8sNamespace }}"
              echo " Deployment : ${{ parameters.k8sDeploymentName }}"
              echo " Container  : ${{ parameters.k8sContainerName }}"
              echo " Image      : ${{ parameters.imageName }}"
              echo "============================================================"

              if [ -z "${{ parameters.imageName }}" ]; then
                echo "❌ ERROR: 'imageName' is required for deployment."
                exit 1
              fi

              # ----------------------------------------------------------------
              # AKS CONTEXT SETUP (uncomment if kubeconfig is not pre-configured)
              # ----------------------------------------------------------------
              # Option A – Service Principal login:
              # az login \
              #   --service-principal \
              #   --username  "$(AKS_CLIENT_ID)" \
              #   --password  "$(AKS_CLIENT_SECRET)" \
              #   --tenant    "$(AKS_TENANT_ID)"
              #
              # Option B – Merge kubeconfig from the AKS cluster:
              # az aks get-credentials \
              #   --resource-group "$(AKS_RESOURCE_GROUP)" \
              #   --name           "$(AKS_CLUSTER_NAME)" \
              #   --overwrite-existing
              # ----------------------------------------------------------------

              echo "🚀 Updating container image on AKS..."
              kubectl -n "${{ parameters.k8sNamespace }}" \
                set image deployment/"${{ parameters.k8sDeploymentName }}" \
                "${{ parameters.k8sContainerName }}"="${{ parameters.imageName }}" \
                --record

              echo "⏳ Waiting for rollout to complete..."
              kubectl -n "${{ parameters.k8sNamespace }}" \
                rollout status deployment/"${{ parameters.k8sDeploymentName }}"

              echo "✅ AKS deployment completed successfully."
            displayName: 'AKS – kubectl set image & rollout status'

      # ========================================================================
      # EKS – Amazon Elastic Kubernetes Service
      # ========================================================================
      # Prerequisites on the agent:
      #   • AWS CLI (aws) installed and configured with appropriate IAM permissions
      #   • kubectl installed
      # To configure kubeconfig for EKS, uncomment and fill in the aws command below.
      # ========================================================================
      - job: Deploy_EKS
        displayName: 'Deploy Supplement Image to EKS (AWS)'
        pool:
          name: RPC-TIBCO-AKS-Pool
        condition: eq('${{ parameters.cloudProvider }}', 'eks')
        steps:
          - script: |
              echo "============================================================"
              echo " EKS Deployment"
              echo " Namespace  : ${{ parameters.k8sNamespace }}"
              echo " Deployment : ${{ parameters.k8sDeploymentName }}"
              echo " Container  : ${{ parameters.k8sContainerName }}"
              echo " Image      : ${{ parameters.imageName }}"
              echo "============================================================"

              if [ -z "${{ parameters.imageName }}" ]; then
                echo "❌ ERROR: 'imageName' is required for deployment."
                exit 1
              fi

              # ----------------------------------------------------------------
              # EKS CONTEXT SETUP (uncomment if kubeconfig is not pre-configured)
              # ----------------------------------------------------------------
              # Option A – Merge kubeconfig from the EKS cluster:
              # aws eks update-kubeconfig \
              #   --name   "$(EKS_CLUSTER_NAME)" \
              #   --region "$(AWS_REGION)"
              #
              # Option B – Assume an IAM role first, then fetch kubeconfig:
              # aws sts assume-role \
              #   --role-arn    "arn:aws:iam::ACCOUNT_ID:role/ROLE_NAME" \
              #   --role-session-name "ado-deploy"
              # aws eks update-kubeconfig \
              #   --name   "$(EKS_CLUSTER_NAME)" \
              #   --region "$(AWS_REGION)"
              # ----------------------------------------------------------------

              echo "🚀 Updating container image on EKS..."
              kubectl -n "${{ parameters.k8sNamespace }}" \
                set image deployment/"${{ parameters.k8sDeploymentName }}" \
                "${{ parameters.k8sContainerName }}"="${{ parameters.imageName }}" \
                --record

              echo "⏳ Waiting for rollout to complete..."
              kubectl -n "${{ parameters.k8sNamespace }}" \
                rollout status deployment/"${{ parameters.k8sDeploymentName }}"

              echo "✅ EKS deployment completed successfully."
            displayName: 'EKS – kubectl set image & rollout status'

      # ========================================================================
      # GKE – Google Kubernetes Engine
      # ========================================================================
      # Prerequisites on the agent:
      #   • Google Cloud SDK (gcloud) installed and authenticated
      #   • kubectl installed (gcloud components install kubectl, if needed)
      # To configure kubeconfig for GKE, uncomment and fill in the gcloud commands below.
      # ========================================================================
      - job: Deploy_GKE
        displayName: 'Deploy Supplement Image to GKE (GCP)'
        pool:
          name: RPC-TIBCO-AKS-Pool
        condition: eq('${{ parameters.cloudProvider }}', 'gke')
        steps:
          - script: |
              echo "============================================================"
              echo " GKE Deployment"
              echo " Namespace  : ${{ parameters.k8sNamespace }}"
              echo " Deployment : ${{ parameters.k8sDeploymentName }}"
              echo " Container  : ${{ parameters.k8sContainerName }}"
              echo " Image      : ${{ parameters.imageName }}"
              echo "============================================================"

              if [ -z "${{ parameters.imageName }}" ]; then
                echo "❌ ERROR: 'imageName' is required for deployment."
                exit 1
              fi

              # ----------------------------------------------------------------
              # GKE CONTEXT SETUP (uncomment if kubeconfig is not pre-configured)
              # ----------------------------------------------------------------
              # Option A – Authenticate with a service account key file:
              # gcloud auth activate-service-account \
              #   --key-file="$(GOOGLE_APPLICATION_CREDENTIALS)"
              #
              # Option B – Merge kubeconfig from the GKE cluster:
              # gcloud container clusters get-credentials "$(GKE_CLUSTER_NAME)" \
              #   --zone    "$(GKE_ZONE)" \
              #   --project "$(GCP_PROJECT_ID)"
              #
              # Option C – Workload Identity Federation (keyless):
              # gcloud auth login --cred-file="$(GOOGLE_APPLICATION_CREDENTIALS)"
              # ----------------------------------------------------------------

              echo "🚀 Updating container image on GKE..."
              kubectl -n "${{ parameters.k8sNamespace }}" \
                set image deployment/"${{ parameters.k8sDeploymentName }}" \
                "${{ parameters.k8sContainerName }}"="${{ parameters.imageName }}" \
                --record

              echo "⏳ Waiting for rollout to complete..."
              kubectl -n "${{ parameters.k8sNamespace }}" \
                rollout status deployment/"${{ parameters.k8sDeploymentName }}"

              echo "✅ GKE deployment completed successfully."
            displayName: 'GKE – kubectl set image & rollout status'
